// Package common_models provides unified forms of structs gotten from the sqlc repository.
// Due to the models being in separate directories, they can't be used interchangeably, hence the need of resolved models
// for identical output.
package common_models
